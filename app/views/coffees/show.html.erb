<h5><%= link_to "Return to Coffee Menu", coffees_path %></h5>

<h1><%= @coffee.blend_name %></h1>

<% if @coffee.rating %>
  <h4>Rated <%= @coffee.rating.round(1) %> out of 5 cups</h4>
<% else %>
  <h4>This coffee has yet to be rated. You can be the first! <%= link_to "write a review.", new_coffee_review_path(@coffee) %></h4>
<% end %>

<h3>Origin: <%= @coffee.origin %> and Variety: <%= @coffee.variety %></h3>

<hr>

<p><%= @coffee.notes %></p>

<h5 class="price">$<%= @coffee.cost %> per pound</h5>

<h4><%= link_to "Reviews of this Coffee Blend", coffee_reviews_path(@coffee) %></h4>

<p>
  <%= link_to "Edit", edit_coffee_path(@coffee) %>
  <%= link_to "Delete", coffee_path(@coffee), data: {
                      confirm: "This will remove the coffee blend completely from the site. Are you confident in this action?",
                      method: "delete"
    } %>
</p>
